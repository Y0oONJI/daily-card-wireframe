# 데일리 카드 와이어프레임 — 작동 흐름 설명

이 문서는 `index.html`(배포 링크: https://y0oonji.github.io/daily-card-wireframe/)이 어떻게 동작하는지 설명합니다. 지금 만든 건 **실제 서버 없이 브라우저 안에서만 동작하는 클릭형 목업**이에요. 진짜 서비스를 만들 때는 "브라우저에 저장"이라고 적힌 부분들을 서버/DB로 옮겨야 합니다. 그 부분은 **7장**에 따로 정리해뒀어요.

---

## 1. 화면 구성

화면은 총 6개 + 부가 요소 2개로 이루어져 있어요.

| 번호 | 화면 | 내용 |
|---|---|---|
| 1 | 오늘의 질문 | 오늘의 질문 텍스트 + "답변하기" 버튼 |
| 2 | 답변 작성 | 질문 다시 보여주고, 텍스트 입력칸 |
| 3 | 배경 선택 | 컬러/그라디언트/이미지 탭 + 카드 미리보기 |
| 4 | 내 카드 결과 | 완성된 카드 + 타인 답변 보기 버튼 |
| 5 | 타인 답변 보기 | 다른 사람 답변 1개 무작위 노출 |
| 6 | 내 기록 | 지금까지 만든 카드를 최신순으로 옆으로 넘겨보기 (1·4·5번 화면에서 진입) |

부가 요소:
- **완료 모달**: 1번 화면 위에 덮이는 팝업. "오늘 이미 답변했다"는 상태일 때만 나타남. 그라데이션 카드(질문+답변)와 "다음 질문까지" 실시간 카운트다운이 있음 (3장 참고).
- **새 질문 토스트**: 화면 위쪽에 잠깐 떴다 사라지는 작은 알림. "날짜가 바뀌어서 새 질문이 왔다"는 걸 알려줌.

---

## 2. 기본 흐름 (오늘 처음 들어왔을 때)

```
1.오늘의 질문 → 2.답변 작성 → 3.배경 선택 → 4.내 카드 결과 → 5.타인 답변 보기
                                                                    ↓
                                                           "처음으로" 누르면 1로 복귀
```

각 단계 동작:
1. **1번**: 오늘 날짜에 해당하는 질문을 보여줌 (질문이 어떻게 정해지는지는 4장에서 설명)
2. **2번**: 사용자가 답을 입력. "제출하기"를 누르면 3번으로 이동 (이 시점엔 아직 저장 안 됨)
3. **3번**: 배경(컬러/그라디언트/이미지)을 고르면 미리보기 카드 배경이 바로 바뀜. "카드 만들기"를 누르는 순간 — **이때 비로소 답변+배경이 저장됨** — 4번으로 이동
4. **4번**: 완성된 카드를 보여줌
5. **5번**: "다른 사람 답변 보기"를 누르면 타인의 답변 1개를 보여줌. "처음으로"를 누르면 1번으로 돌아가는데, 이때 그냥 1번으로 가는 게 아니라 "오늘 이미 답변했는지" 다시 확인하는 절차를 거침 (3장 참고)

---

## 3. 같은 날 다시 들어왔을 때 (재방문)

1번 화면에 진입할 때마다(페이지 새로고침, "처음으로" 클릭 등) 아래 질문을 확인합니다.

> "오늘 날짜로 저장된 답변 기록이 있는가?"

- **있으면**: 1번 화면 위에 **완료 모달**이 덮어씌워짐. 위에서부터: 그라데이션 카드(오늘 질문+답변을 한 장에 같이 보여줌, 배경은 3번에서 고른 색이 아니라 항상 이 보라색 고정), "다른 사람이 남긴 답변 보기"(→5번), "지난 카드 N개 보기"(→6번), "오늘 답변은 완료했어요" 문구, "내일 새 질문까지" 실시간 카운트다운(자정까지 남은 시간, 1초마다 갱신)이 차례로 보임. (1번의 "답변하기" 버튼은 모달에 가려져서 누를 수 없음 — 즉 하루에 한 번만 답변 가능)
- **없으면**: 평소처럼 질문 화면이 보임

이 판단은 **사용자 계정이 아니라 "이 브라우저"를 기준**으로 합니다. 로그인이 없는 구조라서, 다른 기기나 다른 브라우저로 들어오면 "오늘 답변 안 한 사람"으로 보입니다.

---

## 4. 날짜가 바뀌었을 때 (다음 날)

### 질문이 어떻게 정해지는가
질문은 미리 정해둔 풀(지금은 예시로 5개) 안에서, **날짜를 기준으로 순환**하며 정해집니다. 먼저 "2024년 1월 1일부터 오늘까지 며칠 지났는지"를 계산합니다. 그 숫자를 질문 개수(5)로 나눈 나머지가 곧 오늘의 질문 번호예요. 같은 날엔 항상 같은 질문이 나오고, 날짜가 바뀌면 다음 질문으로 자동으로 넘어갑니다.

> 실제 서비스에서는: "질문 20개를 운영자가 직접 등록"하기로 했으니, 이 순환 로직 대신 "오늘 날짜에 매칭된 질문을 DB에서 가져오기"로 바뀌어야 합니다. 지금 이 목업의 5개 질문은 그 자리를 채운 예시일 뿐입니다.

### 새 질문 도착 토스트
"마지막으로 봤던 날짜"를 따로 기억해두고, 1번 화면에 들어올 때마다 오늘 날짜와 비교합니다. 날짜가 달라졌으면(=하루가 지났으면) 화면 위쪽에 "새로운 질문이 도착했어요" 토스트를 잠깐 띄우고, "마지막으로 본 날짜"를 오늘로 갱신합니다. 같은 날 안에서는 한 번 본 뒤로는 다시 뜨지 않습니다.

---

## 5. 내 기록 보기

만든 카드가 하나라도 있으면, **"지난 카드 N개 보기"라는 보조 버튼**이 여러 화면에 나타납니다. 질문에 답하는 도중(2번, 3번)에는 방해가 되니 넣지 않았고, 그 대신 "지금 답을 쓰는 중이 아닌" 화면들에 배치했습니다: 1번(오늘의 질문, "답변하기" 버튼 아래), 4번(내 카드 결과, "다른 사람 답변 보기" 아래), 5번(타인 답변 보기, "처음으로" 위). 어디에 있든 누르면 6번 화면으로 이동합니다. 만든 카드가 하나도 없으면 이 버튼은 아예 보이지 않습니다.

이 화면(6번)에서는:
- **달 단위로 먼저 이동하고, 그 안에서 카드를 옆으로 넘겨봅니다.** 화면 맨 위에 "‹ 2026년 9월 ›" 같은 달 이동 바가 있고, 그 아래 카드 스와이프 영역이 있습니다. 카드가 있는 가장 오래된 달보다 이전으로, 어제가 속한 달보다 이후로는 못 넘어가게 화살표가 흐려지고 막힙니다.
- 카드 스와이프는 일반적인 일기 피드보다는 카드 뭉치를 한 장씩 넘겨보는 느낌에 가깝습니다. **그 달 안에서 날짜 최신순**으로 정렬되며, 화면 가장자리에 다음/이전 카드가 살짝 걸쳐 보여서 "더 넘길 게 있다"는 걸 바로 알 수 있습니다. 좌우 화살표를 누르거나 직접 스와이프해서 넘기고, 하단에 "2 / 5"처럼 **그 달 안에서의** 위치가 표시됩니다. 각 카드는 그날 골랐던 배경 그대로 나옵니다.
- 아직 만든 카드가 하나도 없으면(또는 오늘 처음 만든 게 전부라 어제 이전엔 아무것도 없으면) 달 이동 바 자체가 사라지고 "아직 기록이 없어요" 안내 문구만 뜹니다.
- 예전에는 "오늘 답변"만 브라우저에 저장되고 다음 날 답변하면 어제 것이 덮어써져 사라졌는데, 지금은 날짜별로 배열에 쌓이는 구조라 지나간 카드도 계속 남아 여기서 다시 볼 수 있습니다.

> **왜 달 단위로 나눴는가**: 매일 꾸준히 쓰면 1년에 카드가 최대 365장까지 쌓일 수 있습니다. 예전 버전은 "처음 쓴 날부터 어제까지"를 통째로 한 줄로 이어붙여서, 그 정도 쌓이면 (1) 화면 하나 열 때마다 DOM에 수백 개 카드를 한꺼번에 그려야 했고 (2) "3달 전 그 카드" 하나 찾으려고 수백 장을 순서대로 스와이프해야 하는 문제가 있었습니다. 달 단위로 쪼개면 한 번에 그리는 카드가 최대 31장으로 고정되고, "몇 달 전"으로 바로 점프할 수 있어서 두 문제가 같이 풀립니다.

### 안 쓴 날은 어떻게 보이는가 (기록 정책)

내 기록을 넘겨볼 때, **처음 카드를 만든 날부터 어제까지** 사이에 답을 안 쓴 날이 있으면 그 자리에도 카드가 하나 끼어 있습니다. 다만 진짜 카드처럼 보이면 안 되니 다르게 그렸습니다:

- 배경은 고른 적이 없으니 연한 회색 고정, 카드 전체를 흐리게(투명도를 낮춤) 처리해서 실제로 고른 배경이 무슨 색이든 항상 "꺼져있다"는 게 구분되게 했습니다. 테두리도 점선입니다.
- 그날의 질문은 그대로 보여줍니다 (그날엔 이런 질문이었구나, 정도는 알 수 있게)
- 답변 자리엔 "작성하지 않았어요"만 뜨고, **지금 와서 그 날짜의 답을 채워 넣을 수는 없습니다** — 답변은 그날 하루만 가능하다는 원칙은 그대로입니다
- 즉 "안 썼다"는 사실 자체를 숨기거나 지우진 않되, 실패했다는 낙인처럼 보이지 않게 만드는 게 목적입니다. 오늘 안 써도 그 카드가 사라지거나 벌점처럼 남는 게 아니라, 그냥 "이 날은 비어있었네" 정도로만 스쳐 지나가게 하는 것.

> **미해결 — 빈 카드의 시작점 기준**: 지금은 "이 사람이 처음으로 답변을 쓴 날"부터 빈 카드를 채웁니다. 근데 엄밀히는 "가입일(또는 앱을 처음 연 날)"부터가 더 맞을 수 있습니다 — 가입은 1월 1일에 했는데 답변은 1월 10일에 처음 썼다면, 지금 로직은 1~9일을 아예 없던 기간처럼 건너뜁니다. 이 목업엔 회원가입/설치 시점 데이터가 없어서 "첫 답변일"로 근사해뒀는데, 실제 서비스에서 가입일 데이터가 생기면 그 기준으로 바꾸는 걸 권장합니다.

> 데모용 참고: 5일 전 · 3일 전 · 1일 전 자리에 항상 더미 카드 3개가 채워져 있습니다. 페이지를 열 때마다, 그리고 "데모 초기화"를 눌러도 다시 채워집니다(이미 있는 날짜는 건너뛰고 없는 날짜만 채우는 방식이라, 실제로 답변을 써도 중복되진 않음). "내 기록" 기능이 절대 비어 보이지 않게 하기 위한 임시 데이터로, 실제 서비스에는 없어야 하는 코드입니다.

---

## 6. 데모용 버튼 (실제 서비스엔 없음)

화면 맨 아래에 작게 있는 두 버튼은 **팀원이 테스트하기 편하도록 임시로 넣은 것**이며, 실제 출시 버전에는 빼야 합니다.

- **⏭ 다음날로 이동**: 실제 자정을 기다리지 않고 "하루가 지난 것처럼" 흉내 냅니다. 이걸 누르면 완료 모달이 사라지고 새 질문 + 토스트가 뜹니다.
- **⟲ 데모 초기화**: 흉내 낸 날짜와 오늘 답변 기록을 모두 지우고 맨 처음 상태로 되돌립니다.

---

## 7. 지금 "브라우저에 저장"되는 것들 (실제 서비스에선 서버로 옮겨야 함)

| 저장 항목 | 지금 (목업) | 실제 서비스에서는 |
|---|---|---|
| 지금까지 작성한 답변 + 배경 (날짜별 기록) | 브라우저 localStorage에 배열로 저장 | 서버 DB에 저장, 익명 식별자 단위로 |
| 타인 답변 노출 | 항상 같은 문구 하나로 고정 | DB에서 같은 질문에 달린 답변 중 무작위 1개 조회 |
| 욕설 필터 | 없음 (목업이라 생략) | 제출 시 서버에서 금칙어 검사 후 차단 |
| 질문 목록 | 코드 안에 5개 하드코딩 | 운영자가 등록한 질문 DB에서 날짜로 조회 |
| 날짜 저장 형식 | `2026-09-02` (ISO 형식 문자열) | 그대로 서버 DB 컬럼에 써도 되는 형식으로 맞춰뒀음 |
| 배경 저장 형식 | CSS 값 그대로 문자열로 저장 (`linear-gradient(...)`) | 나중에 배경 팔레트를 바꿀 걸 대비해서, 실제로는 CSS 값이 아니라 `"gradient-2"` 같은 코드/ID로 저장하고 화면에 그릴 때만 앱이 실제 CSS 값으로 변환하는 걸 권장 |

---

## 한눈에 보는 전체 분기도

```
사용자가 1번 화면(=앱 진입)에 들어옴
  │
  ├─ [항상 체크 1] 마지막으로 본 날짜와 오늘이 다른가?
  │     ├─ 다름 → "새로운 질문이 도착했어요" 토스트 표시
  │     └─ 같음 → 아무 알림 없음
  │
  ├─ [항상 체크 2] 오늘 답변한 기록이 있는가?
  │     ├─ 있음 → 완료 모달 표시 (오늘 카드 + 카운트다운, [다른 사람이 남긴 답변 보기]→5번 / [지난 카드 보기]→6번)
  │     └─ 없음 → 질문 화면 정상 표시
  │
  └─ (답변 안 한 경우) 답변하기 → 2 → 3 → 4 → 5 → 처음으로 (위 분기 다시 탐)
```
